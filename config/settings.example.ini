[postgres]
host = postgres
port = 5432
user = airflow
password = airflow
database = airflow

[paths]
raw_dir = /opt/airflow/data/raw
clean_dir = /opt/airflow/data/clean

[noaa]
bucket = noaa-isd-pds
prefix = isd-lite/data

[pipeline]
batch_size = 500  # files per COPY batch
min_hourly_records = 4
